# 🚀 دليل النشر على Render (الأقرب لـ Vercel)

Render هو الخيار الأفضل إذا كنت تريد تجربة مشابهة لـ Vercel لكن مع دعم كامل لتطبيقات Python الخلفية مثل GojoXNG.

---

## 📋 الخطوات

### 1️⃣ اذهب إلى Render
- افتح **https://render.com**
- سجّل دخول بحساب GitHub

### 2️⃣ أنشئ خدمة جديدة
- اضغط **New +** → **Blueprint**
- اختر مستودع GitHub: `mkj555m5/gojoxng`
- Render سيكتشف ملف `render.yaml` تلقائياً ويعرض الإعدادات

### 3️⃣ راجع الإعدادات
ستظهر لك الإعدادات من `render.yaml`:
- **Name**: gojoxng
- **Runtime**: Python 3.12
- **Plan**: Free (يمكنك الترقية لاحقاً)
- **Region**: Oregon

### 4️⃣ اضبط المتغيرات البيئية
Render سيولّد `SEARXNG_SECRET` تلقائياً. فقط اضبط:

| Variable | Value | ملاحظة |
|----------|-------|--------|
| `SEARXNG_SECRET` | (توليد تلقائي) | ✅ Render يولّده |
| `SEARXNG_BIND_ADDRESS` | `0.0.0.0` | ✅ مضبوط مسبقاً |
| `SEARXNG_BASE_URL` | `https://gojoxng.onrender.com/` | اضبط بعد النشر الأول |

### 5️⃣ اضغط Apply / Create
- Render سيبدأ البناء (يستغرق 3-5 دقائق)
- تابع السجلات في تبويب **Events** أو **Logs**

### 6️⃣ احصل على الرابط
- بعد اكتمال النشر، اذهب إلى خدمتك → ستجد الرابط في الأعلى
- سيكون مثل: `https://gojoxng-xxxx.onrender.com`

### 7️⃣ حدّث SEARXNG_BASE_URL
- اذهب إلى **Environment** → عدّل `SEARXNG_BASE_URL` بالرابط الذي حصلت عليه
- **Save Changes** → سيتم إعادة النشر تلقائياً

---

## ⚠️ ملاحظات مهمة

### خطة Free المجانية:
- 🕐 **15 دقيقة** من عدم النشاط → الخدمة تنام
- ⏰ عند الطلب التالي → تستيقف خلال ~30 ثانية (cold start)
- 📊 **750 ساعة** شهرياً (يكفي لخدمة واحدة تعمل 24/7)

### للنشر الإنتاجي (مستحسن):
- ترقية إلى خطة **Starter** ($7/شهر) → لا تنام الخدمة + أداء أفضل

---

## 🔧 استكشاف الأخطاء

**الخدمة لا تبدأ؟**
- تحقق من **Logs** في Render
- تأكد أن `SEARXNG_BIND_ADDRESS=0.0.0.0`

**البحث لا يرجع نتائج؟**
- بعض محركات البحث تحظر IPs الخاصة بمراكز البيانات
- هذا طبيعي — جرّب محركات مختلفة في الإعدادات

**الشعار لا يظهر؟**
- تحقق من مسار الملف: `searx/static/themes/simple/img/gojoxng-logo.png`

---

## 🆚 مقارنة: Render vs Railway vs Vercel

| الميزة | Vercel | Render | Railway |
|--------|--------|--------|---------|
| Python backend | ❌ محدود | ✅ كامل | ✅ كامل |
| Docker | ❌ | ✅ | ✅ |
| Granian/WSGI | ❌ | ✅ | ✅ |
| وقت الطلب | 10 ثوان | غير محدود | غير محدود |
| Valkey/Redis | ❌ | ✅ (إضافي) | ✅ |
| Free tier | نعم | نعم | $5 رصيد |
| سهولة الاستخدام | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## ✅ الخلاصة

**لا تستخدم Vercel** لمحرك بحث مثل GojoXNG.

**استخدم Render** إذا كنت تريد تجربة مشابهة لـ Vercel.
**استخدم Railway** إذا كنت تريد إعداداً أسرع (تم إعداده بالفعل).
